import React from 'react'
import { useAuth } from '../context/AuthContext';

// const {user} = useAuth();
const Dashboard = () => {
  return (
    <div>
      {/* {user?"No User":user.email}; */}
    </div>
  )
}

export default Dashboard
